<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Dashboard - Fichaje</title>
  <link rel="stylesheet" href="/css/bootstrap.min.css">
  <script src="/js/bootstrap.bundle.min.js"></script>
</head>
  
<body>
  <%- include('partials/menu') %>

  <div class="container mt-5">
    <div class="row">
      <div class="col-md-12">
        <h1 class="text-center">
          ¡Bienvenid@, <%= empleado?.nombre || 'Usuario' %> <%= empleado?.apellidos || '' %>!
        </h1>
        <h2 class="text-center">Registro de Fichaje</h2>
      </div>
    </div>

    <% if (mensaje) { %>
      <div class="alert alert-<%= tipoMensaje === 'exito' ? 'success' : 'danger' %>" role="alert">
        <%= mensaje %>
      </div>
    <% } %>

    <div class="row mt-4">
      <div class="col-md-6">
        <form action="/api/empleados/<%= empleado?.id %>/entrada" method="POST">
          <button type="submit" class="btn btn-success btn-block w-100">Registrar Entrada</button>
        </form>
      </div>
      <div class="col-md-6">
        <form action="/api/empleados/<%= empleado?.id %>/salida" method="POST">
          <button type="submit" class="btn btn-danger btn-block w-100">Registrar Salida</button>
        </form>
      </div>
    </div>
  </div>
</body>
</html>